--- a/frontend/src/components/LineSidebar.jsx
+++ b/frontend/src/components/LineSidebar.jsx
@@ -1,4 +1,5 @@
 import { Fragment, useRef, useState, useCallback, useEffect } from 'react';
+import { createPortal } from 'react-dom';
 import './LineSidebar.css';
 
 const FALLOFF_CURVES = {
@@ -119,8 +120,7 @@
   const handleContextMenu = useCallback((e, index) => {
     e.preventDefault();
     e.stopPropagation();
-    const rect = e.currentTarget.getBoundingClientRect();
-    setMenu({ index, x: e.clientX - rect.left, y: e.clientY - rect.top });
+    setMenu({ index, x: e.clientX, y: e.clientY });
   }, []);
 
   useEffect(() => {
@@ -180,36 +180,6 @@
                 <span className="line-sidebar__text">{label}</span>
                 {stars[index] && <span className="line-sidebar__star" aria-label="즐겨찾기">★</span>}
               </span>
-              {menu && menu.index === index && (
-                <div
-                  className="absolute z-40 min-w-[9rem] overflow-hidden rounded-lg border border-white/15 bg-neutral-900/95 shadow-xl backdrop-blur"
-                  style={{ left: menu.x, top: menu.y }}
-                  onClick={e => e.stopPropagation()}
-                >
-                  <button
-                    type="button"
-                    onClick={e => {
-                      e.stopPropagation();
-                      onItemFavorite?.(index);
-                      setMenu(null);
-                    }}
-                    className="flex w-full items-center gap-2 whitespace-nowrap px-4 py-2 text-left text-sm text-yellow-300 transition-colors hover:bg-yellow-400/15"
-                  >
-                    {stars[index] ? '★ 즐겨찾기 해제' : '☆ 즐겨찾기 추가'}
-                  </button>
-                  <button
-                    type="button"
-                    onClick={e => {
-                      e.stopPropagation();
-                      onItemDelete?.(index);
-                      setMenu(null);
-                    }}
-                    className="flex w-full items-center gap-2 whitespace-nowrap px-4 py-2 text-left text-sm text-red-300 transition-colors hover:bg-red-500/20"
-                  >
-                    🗑 대화 삭제
-                  </button>
-                </div>
-              )}
             </li>
             {separatorAfter != null && index === separatorAfter - 1 && (
               <li className="line-sidebar__separator" aria-hidden="true" />
@@ -217,6 +187,38 @@
           </Fragment>
         ))}
       </ul>
+      {menu &&
+        createPortal(
+          <div
+            className="fixed z-[100] min-w-[9rem] overflow-hidden rounded-lg border border-white/15 bg-neutral-900/95 shadow-xl backdrop-blur"
+            style={{ left: menu.x, top: menu.y }}
+            onClick={e => e.stopPropagation()}
+          >
+            <button
+              type="button"
+              onClick={e => {
+                e.stopPropagation();
+                onItemFavorite?.(menu.index);
+                setMenu(null);
+              }}
+              className="flex w-full items-center gap-2 whitespace-nowrap px-4 py-2 text-left text-sm text-yellow-300 transition-colors hover:bg-yellow-400/15"
+            >
+              {stars[menu.index] ? '★ 즐겨찾기 해제' : '☆ 즐겨찾기 추가'}
+            </button>
+            <button
+              type="button"
+              onClick={e => {
+                e.stopPropagation();
+                onItemDelete?.(menu.index);
+                setMenu(null);
+              }}
+              className="flex w-full items-center gap-2 whitespace-nowrap px-4 py-2 text-left text-sm text-red-300 transition-colors hover:bg-red-500/20"
+            >
+              🗑 대화 삭제
+            </button>
+          </div>,
+          document.body,
+        )}
     </nav>
   );
 };
--- a/frontend/src/components/Sidebar.jsx
+++ b/frontend/src/components/Sidebar.jsx
@@ -19,6 +19,7 @@
   return (
     <motion.aside
       initial={false}
+      onContextMenu={(e) => e.preventDefault()}
       animate={{ x: visible ? 0 : '-120%', opacity: visible ? 1 : 0 }}
       transition={{ duration: 0.5, ease: [0.16, 1, 0.3, 1] }}
       className="fixed bottom-4 left-4 top-24 z-20 hidden w-64 flex-col rounded-2xl border border-white/15 bg-white/10 p-4 shadow-lg shadow-black/20 backdrop-blur-lg lg:flex"
--- a/frontend/src/components/LineSidebar.css
+++ b/frontend/src/components/LineSidebar.css
@@ -31,6 +31,12 @@
 .line-sidebar__item {
   position: relative;
   cursor: pointer;
+  padding: 0.5rem 0.4rem;
+  border-radius: 8px;
+  transition: background-color 0.15s ease;
+}
+.line-sidebar__item:hover {
+  background: rgba(255, 255, 255, 0.06);
 }
 
 .line-sidebar__item::before {
